import { LoginScreen } from '../components/login/components/LoginScreen';

/**
 * LOGIN VIEW (equivalente a views/ en Vue)
 * 
 * En Expo Router, los archivos en /app son las "views"
 * Para acceder: navega a /login en tu app
 */
export default function Login() {
  return <LoginScreen />;
}
